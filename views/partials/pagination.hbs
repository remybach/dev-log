{{#if pagination}}
  <nav class="pagination">
    {{#if pagination.prev}}
      <a href="{{pagination.prev}}">&lt; Previous Page</a>
    {{/if}}
    {{#if pagination.next}}
      <a href="{{pagination.next}}">Next Page &gt;</a>
    {{/if}}
  </nav>
{{/if}}